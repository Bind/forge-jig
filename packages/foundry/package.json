{
  "name": "foundry",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "jest",
    "dev": "tsc --watch",
    "build": "tsc",
    "lint": "TIMING=1 eslint ./*.ts --fix",
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "devDependencies": {
    "config": "workspace:*",
    "jest": "^27.5.1",
    "tsconfig": "workspace:*",
    "typescript": "^4.6.3"
  },
  "jest": {
    "preset": "config/jest/node"
  },
  "dependencies": {
    "toml": "^3.0.0"
  }
}
