{
  "name": "@forge-jig/foundry",
  "version": "0.1.5",
  "private": false,
  "license": "MIT",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "jest",
    "dev": "tsc --watch",
    "build": "tsc",
    "lint": "TIMING=1 eslint ./*.ts --fix",
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "devDependencies": {
    "@forge-jig/config": "workspace:*",
    "@forge-jig/tsconfig": "workspace:*",
    "jest": "^27.5.1",
    "typescript": "^4.6.3"
  },
  "jest": {
    "preset": "config/jest/node"
  },
  "dependencies": {
    "toml": "^3.0.0"
  }
}
